{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": "src", // src 폴더를 기준으로 경로 설정
    "paths": {
      "@/*": ["*"] // @는 src 폴더를 기준으로 경로를 찾도록 설정
    }
  }
}
